/** @type {import('next').NextConfig} */
const nextConfig = {
  // The dev-tools badge defaults to bottom-left, where it covers the
  // sidebar footer buttons (theme toggle / reset) during development.
  devIndicators: {
    position: "bottom-right",
  },
};

export default nextConfig;
