# NEXUS

개인 자산 포트폴리오 관리 웹 앱 (Next.js App Router + TypeScript + Tailwind CSS). 주식(국내/해외)·채권·현금·연금(DC/IRP)·기타(사용자 정의 카테고리) 자산의 보유 현황을 관리하고 (매수/매도 거래 기록이 아닌, 현재 상태를 직접 입력·수정하는 방식), 배당 수령 기록과 자산 추이·수익률 분석을 제공합니다. PC(데스크톱) 브라우저에 최적화된 사이드바 레이아웃과 토스(Toss) 스타일의 미니멀한 다크/라이트 모드를 지원합니다.

## 로컬 실행

**요구사항: Node.js 22.13 이상** (SQLite 데이터 계층이 Node 내장 `node:sqlite` 모듈을 사용합니다. `node -v`로 확인하세요.)

```bash
npm install
npm run dev
```

브라우저에서 http://localhost:3000 접속. 최초 실행 시 `data/nexus.db` (SQLite, git에 커밋되지 않음)가 자동 생성되며, **데이터는 비어 있는 상태로 시작**하고 각 화면의 온보딩/빈 상태가 안내합니다.

둘러보기용 데모 데이터를 채우려면 시딩을 명시적으로 켜세요:

```bash
NEXUS_SEED=1 npm run dev
```

### 환경변수

| 변수 | 기본값 | 설명 |
| --- | --- | --- |
| `NEXUS_DB_PATH` | `<cwd>/data/nexus.db` | SQLite 파일 경로. **실제 데이터와 분리된 DB로 실험·테스트할 때 지정하세요.** |
| `NEXUS_SEED` | (없음) | `1`이면 빈 DB에 데모 자산·배당·추이 이력을 시딩합니다. |
| `QUOTE_API_BASE` | 야후 파이낸스 | 시세·배당 조회 업스트림(목 서버 지정용). |
| `NEWS_API_BASE` | 각 언론사 RSS | 뉴스 RSS 업스트림(목 서버 지정용). |
| `DATA_LAYER` | (없음) | `mock`이면 파일 DB 대신 인메모리 계층 사용. |

> **데이터 안전 관련 설계 노트.** DB 경로는 `NEXUS_DB_PATH`가 없으면 `process.cwd()` 기준이라, 서버를 다른 디렉터리에서 띄우면 *다른* 빈 DB가 열립니다. 예전에는 이때 빈 테이블을 보고 데모 데이터를 자동으로 심어서 **"실제 데이터가 지워지고 샘플로 바뀐 것처럼" 보였습니다.** 지금은 시딩이 옵트인(`NEXUS_SEED=1`)이라 이런 경우 그냥 빈 화면이 뜨고, 원래 경로로 다시 띄우면 데이터가 그대로 돌아옵니다. 명시적 동작(`/api/assets/reset`, 백업 가져오기)은 이 플래그와 무관하게 그대로 동작합니다.

> Node 22.13 미만에서 실행하면 SQLite 대신 인메모리 데이터 계층으로 자동 폴백됩니다 — 앱은 동작하지만 **서버 재시작 시 데이터가 사라집니다** (서버 콘솔에 경고 출력). 영속 저장이 필요하면 Node를 업그레이드하세요.

## 자동 테스트 (vitest)

```bash
npm test          # 1회 실행
npm run test:watch # 변경 감지 모드
```

이 앱은 **돈을 계산합니다** — 세액, 목표 비중 대비 괴리, 배당 예상액이 화면 그대로 판단 근거가 되므로, 계산 계층은 눈으로 확인하는 대신 테스트로 고정해 둡니다. 테스트 대상은 `src/lib/services`의 **순수 함수**뿐이며, 파일은 대상 모듈 옆에 `*.test.ts`로 둡니다.

| 파일 | 고정해 둔 것 |
| --- | --- |
| `tax-service.test.ts` | 양도세 기본공제는 **연간 합산액에 한 번만** 적용 · 기실현 손실이 이후 이익을 상계 · 이미 확정된 세금과 추가 매도의 한계 세금 분리 · 금융소득 2천만원 기준(현재/연말 예상 각각) · 연금저축 600만 → 합산 900만 순서로 한도 적용 · **연금 수령**: 손검증한 균등 수령액·연 1,500만원 한도 경계(딱 한도면 저율 유지)·초과 시 한도 아래로 내리는 최소 수령 기간 역산 |
| `rebalance-service.test.ts` | 5/25 룰(절대 밴드와 목표의 25% 중 **더 좁은 쪽**) · 목표 0% 구간은 절대 밴드 · `excludeHome`이 실거주 주택만 제외 · 추가 납입 배분이 **정확히 납입액만큼** 나뉨 |
| `portfolio-service.test.ts` | 자산 타입별 원금/평가금액 · USD 환산(수익률은 환율에 불변) · 원금 0일 때 NaN 대신 0% · 비중 합계 100% |
| `stock-analysis-service.test.ts` | 세부 섹터별 분리 집계(`기술 (반도체)` vs `기술 (AI SW)`)와 기본 섹터 유지 · 시장 기준 국내/해외 판정 · 집중도(HHI·유효 종목수) · 손익 기여도의 부호 |
| `dividend-service.test.ts` | 최근 12개월 버킷(빈 달 0으로 채움) · 창 밖 기록 제외 · **올해 누계와 최근 12개월은 다른 값** |
| `concurrency.test.ts` | 동시 실행 풀이 상한을 넘지 않음 · 결과가 **완료 순서가 아닌 입력 순서** · 항목당 정확히 한 번 실행 · 잘못된 상한은 1로 폴백 |
| `tax-inputs.test.ts` | 저장된 세금 입력값 정규화(음수·비숫자·잘못된 연도/소유자 제거) · 과거 5년만 보관 · 패치가 다른 연도·소유자를 건드리지 않음 · **새 연도는 작년 값을 물려받지 않음** |
| `asset-form.test.ts` | 자산 폼이 **무엇을 저장하는지** — 현재가 우선순위(시세 → 평단가 → 저장값, 단 통화가 바뀌면 저장값 무효) · 섹터 우선순위(수동 → 자동 조회 → 기존) · 기본 섹터 없는 세부 섹터는 버림 · 현재 평가액 미입력은 원금과 동일 · 타입 전환 시 다른 타입 필드가 딸려오지 않음 |
| `retirement-service.test.ts` | 은퇴 계산의 **돈 계산** — 실질 수익률 0% 케이스의 손검증(적립 합계·필요 자산·소진 나이 65세·세 레버) · 실질<명목(물가) · 시나리오 보수<기본<낙관 · **저축 페이스는 중앙값이라 일회성 급등에 안 흔들림**·급등 시 표시 |
| `retirement.test.ts` | 은퇴 입력값 정규화(출생연도 범위·나이/수익률 클램프·기대수명>은퇴나이) · 알 수 없는 스코프 제거 · 패치가 다른 스코프를 안 건드림 |
| `action-center-service.test.ts` | 리밸런싱 항목 최대 3개 상한 · 심각도 → 카테고리(세금·연금 우선) 정렬 · 세금은 소유자별 개별 판정(JOINT 제외) · 연말 전용 항목과 14일 배당 알림의 날짜 경계 |

날짜에 의존하는 로직(연말 세무 점검, 배당 D-day, 월별 버킷)은 `vi.setSystemTime`으로 시계를 고정해 실행 시점과 무관하게 같은 결과가 나오도록 했습니다.

외부 데이터에 의존하는 모듈(`quote-service`, `news-service`, `dividend-forecast-service`, `backup-service`)은 `server-only`이고 네트워크·DB I/O가 본질이라 이 스위트에서 제외했습니다 — 목 서버를 띄운 수동 검증으로 다룹니다.

## 폴더 구조

```
src/
  app/                        # 라우트 (App Router)
    page.tsx                  # 대시보드
    portfolio/page.tsx        # 포트폴리오 목록
    analytics/page.tsx        # 분석 (수익률·낙폭·카테고리 손익·성장 추이)
    dividends/page.tsx        # 배당 (기록 입력·월별 차트·통계)
    settings/page.tsx         # 설정 (소유자 이름 커스터마이즈 · 데이터 백업)
    api/dividends/             # 배당 REST API (GET/POST, [id] DELETE)
    api/settings/              # 앱 설정 REST API (GET / PUT)
    api/backup/                # 백업 내보내기(GET) / 가져오기(import POST)
    api/assets/                # REST API (Route Handlers)
      route.ts                 # GET(목록)/POST(생성)
      [id]/route.ts             # PATCH(수정)/DELETE(삭제)
      reset/route.ts            # POST(데모 데이터로 초기화, 개발용)
    api/snapshots/route.ts     # GET(일별 자산 히스토리; 오늘 포인트 자동 기록)
    api/quote/route.ts         # GET(티커 현재가 조회 — 야후 파이낸스, 원화 환산)
  components/
    layout/                   # Sidebar (내비게이션 + 자산 추가 + 테마/초기화)
    dashboard/                # 대시보드 위젯 (요약/자산배분/미리보기)
    portfolio/                # AssetTable (자산 목록 테이블)
    asset/                    # 자산 추가/수정 다이얼로그
      AssetFormModal.tsx        # 다이얼로그 프레임 + 레이아웃만
      use-asset-form.ts         # 폼 상태 한 덩어리 · 시세 조회 · 저장/삭제
      field-styles.ts           # 필드 공통 클래스
      fields/                   # 타입별 필드 그룹 (Common/Stock/Value)
    common/                   # Toast 등 공통 UI
  lib/
    models/                   # 도메인 모델 (자산 타입 정의) — 확장의 시작점
      asset.ts                  # AssetType, CashAsset, StockAsset, Asset(discriminated union)
      asset-types.ts             # 타입별 라벨/표시 순서
      snapshot.ts                # PortfolioSnapshot (일별 자산 히스토리 포인트)
      seed-data.ts               # 데모 데이터
      validate-asset-input.ts    # API 요청 바디 검증
    services/
      portfolio-service.ts      # 손익/평가금액/자산배분 계산 (순수 함수, DB 무관)
      *.test.ts                 # 계산 계층 단위 테스트 (vitest, 대상 모듈과 같은 폴더)
      snapshot-service.ts       # 스냅샷 계산 + 데모 히스토리 생성 (순수 함수)
      quote-service.ts          # 외부 시세 조회 (야후 파이낸스, USD→KRW 환산, 5분 캐시)
    repositories/               # 데이터 접근 계층 (교체 가능)
      asset-repository.ts        # AssetRepository 인터페이스
      sqlite-asset-repository.ts # SQLite 구현체 (기본값)
      mock-asset-repository.ts   # 인메모리 목업 구현체
      snapshot-repository.ts     # SnapshotRepository 인터페이스 (+ sqlite/mock 구현체)
      snapshot-sync.ts           # 서버 오케스트레이션: 오늘 스냅샷 기록/히스토리 시딩
      index.ts                   # getAssetRepository()/getSnapshotRepository() 팩토리
    db/
      client.ts                  # SQLite(DatabaseSync) 커넥션 + 스키마 초기화
    hooks/
      use-assets.ts              # 클라이언트: /api/assets CRUD 훅
      use-snapshots.ts           # 클라이언트: /api/snapshots 히스토리 훅
    portfolio-context.tsx       # 전역 상태(Context): assets, summary, CRUD 함수
    asset-modal-context.tsx     # 자산 추가/수정 모달 열림 상태, 토스트
    theme-provider.tsx          # 다크/라이트 모드 전환
    format.ts                   # 통화/퍼센트 포맷터
```

## 데이터 모델 — 자산 타입 확장하기

모든 자산은 `src/lib/models/asset.ts`의 discriminated union `Asset = CashAsset | StockAsset | BondAsset | PensionAsset | CustomAsset`로 정의됩니다. 채권(BOND)과 연금(PENSION)은 실제로 이 절차대로 추가해 설계를 검증했으며, 새 타입 추가 절차는 다음과 같습니다:

**컴파일러가 강제하는 단계** (빠뜨리면 `tsc`가 알려줌):

1. `AssetType`에 새 타입 문자열 추가 (예: `"PENSION"`)
2. `BaseAsset`을 확장한 인터페이스 추가 후 `Asset`·`AssetInput` 유니온에 포함
3. `asset-types.ts`에 라벨·표시 순서 추가
4. `portfolio-service.ts`의 `getAssetMetrics`에 case 추가 (`assertNever` exhaustiveness)
5. `AllocationBreakdown.tsx`의 `CATEGORY_COLOR`에 색 추가 — 새 색은 dataviz 검증(라이트/다크 서페이스, 색각 이상 구분)을 통과시킬 것

**컴파일러가 못 잡는 단계** (안전하게 실패하지만 조용히 빠짐 — 직접 챙길 것):

6. `validate-asset-input.ts`에 런타임 검증 branch 추가 — 없으면 새 타입 생성 요청이 400으로 거부됨
7. 입력 폼에 필드 추가 — 없으면 UI에서 새 타입을 선택할 수 없음. 두 군데를 손봅니다: **저장 규칙**은 `models/asset-form.ts`의 `buildAssetInput`에 case를 추가하고(초기값이 필요하면 `initialFormValues`도), **화면**은 `components/asset/fields/`에 필드 그룹 컴포넌트를 만들어 `AssetFormModal.tsx`에서 타입별로 렌더링합니다. 금액 한 쌍(원금 + 현재 평가액)만 필요하다면 기존 `AmountPairFields`를 재사용하면 됩니다
8. (선택) `seed-data.ts`에 데모 데이터 추가

**리포지토리·API 코드는 자산 타입이 늘어나도 구조를 바꿀 필요가 없습니다.** SQLite/목업 리포지토리 둘 다 타입별 필드를 `payload` JSON으로 저장하므로 새 컬럼이나 마이그레이션이 필요 없습니다 (BOND 추가 시에도 마이그레이션 0건이었음). 히스토리 스냅샷의 타입별 구성(`byType`)도 자동으로 새 타입을 포함합니다.

## 데이터 계층 — SQLite / Mock 교체

`src/lib/repositories/index.ts`의 `getAssetRepository()`가 유일한 진입점입니다. 기본값은 파일 기반 SQLite(Node 내장 `node:sqlite`, 네이티브 애드온 불필요)이며, 아래처럼 환경변수로 순수 인메모리 목업으로 바꿀 수 있습니다 (프로세스 재시작 시 초기화됨 — 테스트/데모용):

```bash
DATA_LAYER=mock npm run dev
```

나중에 실제 서버 DB(Postgres 등)로 옮길 때도 `AssetRepository` 인터페이스를 구현하는 새 클래스만 추가하면 되고, API 라우트나 컴포넌트는 그대로 둘 수 있습니다.

## 언어 전환 (한국어/English)

사이드바 하단의 **언어 아이콘(KO/EN)** 으로 한국어와 영어를 전환할 수 있습니다. 선택값은 `localStorage`에 저장되어 새로고침 후에도 유지됩니다. 구현은 **한국어 원문을 키로 쓰는 경량 i18n**(`src/lib/i18n/`)으로, `t("한국어")`가 한국어일 땐 그대로, 영어일 땐 사전(`translations.ts`)에서 번역을 찾고 **없으면 한국어로 자연스럽게 폴백**합니다(화면이 절대 깨지지 않음). 내비게이션·페이지 제목·카드·표·설정·폼·빈 상태·토스트 등 사용자에게 보이는 문구를 번역하며, 소유자 이름처럼 사용자가 직접 입력한 값과 종목명 같은 데이터는 그대로 둡니다.

## 다크/라이트 모드

**설정 → 화면 테마**에서 **시스템 / 라이트 / 다크** 3가지 중 선택할 수 있습니다. 눈이 편한 다크 모드를 명시적으로 고정하거나, &lsquo;시스템&rsquo;을 선택하면 기기의 다크 모드 설정을 실시간으로 따라갑니다(OS 설정이 바뀌면 즉시 반영). 사이드바 하단의 해/달 아이콘은 라이트↔다크 빠른 전환 단축 버튼입니다. 선택값은 `localStorage`에 저장되고, 하이드레이션 전 인라인 스크립트로 클래스를 미리 설정해 깜빡임이 없습니다. Tailwind `darkMode: "class"` 전략을 사용합니다.

## 화면 구성 (데스크톱)

- **좌측 사이드바**: 대시보드/포트폴리오/분석/**주식**/배당/**세금**/**은퇴**/**뉴스**/설정 내비게이션, "자산 추가" 버튼, 하단에 **금액 숨기기(프라이버시)**·테마 전환·언어 전환 버튼
- **외부 데이터 실패 표시**: 시세·배당 이력·뉴스처럼 외부에서 가져오는 값은 실패해도 앱이 죽지 않게 폴백되는데, 예전에는 그 폴백이 **너무 조용해서** 사용자가 "정말 값이 없는 것"과 "조회에 실패한 것"을 구분할 수 없었습니다. 특히 세금 화면의 연말 예상 배당은 조회 실패 시 실제보다 **작게** 나오면서 아무 경고가 없어, 종합과세 기준을 넘을 상황을 안 넘는 것으로 오판할 수 있었습니다. 이제 배당 예측·뉴스·밸류에이션·시세 이력 훅이 모두 `hasError`를 노출하고, 공용 `DataErrorNotice`(앰버 경고 스트립)로 해당 카드에 표시합니다. 핵심은 **HTTP 상태가 아니라 페이로드로 판정**한다는 점입니다 — 이 라우트들은 개별 티커/출처 실패를 삼키고 200을 반환하므로, `/api/stocks/valuations`·`/api/stocks/history`가 실패한 티커를 `failed` 배열로 함께 내려주도록 바꾸고, 배당 예측은 `failedTickers`가 있으면서 결과가 0건일 때, 뉴스는 모든 출처가 실패했을 때를 "전체 실패"로 판정합니다. **부분 실패는 기존의 "조회 실패: 종목명…" 안내로만 표시**해 정상 데이터가 있는데 전체 경고가 뜨는 오탐을 막았습니다.
- **배당 예측 성능**: 배당 예측은 보유 종목 **하나당 외부 조회 한 번**이라 이 앱에서 가장 비싼 호출인데, 예전에는 이를 `for` 루프에서 하나씩 순서대로 기다려 총 소요 시간이 종목 수 × 왕복 시간의 **합**이었습니다. 이제 작은 동시 실행 풀(`mapWithConcurrency`, 6개씩)로 묶어 처리합니다 — 한꺼번에 다 던지지 않는 이유는 키 없는 비공식 업스트림이 레이트 리밋을 걸기 때문이고, 결과는 완료 순서와 무관하게 **입력 순서로 모아** 정렬·실패 목록이 매번 동일하게 나오도록 했습니다. 같은 종목을 부부가 나눠 보유한 경우처럼 **티커가 겹치면 요청도 하나로 합칩니다**(`fetchDividendHistory`의 in-flight 공유 — 기존 1시간 TTL 캐시는 조회가 *끝난 뒤* 도착한 호출만 도와주므로, 병렬화하면 동시에 출발한 같은 티커 두 건이 둘 다 캐시를 놓치기 때문). 그리고 대시보드·배당·세금 **세 화면이 같은 예측을 쓰는데 각자 따로 불러오던 것**을, 훅 모듈 수준 캐시(5분 TTL)로 공유해 화면을 오갈 때 재조회하지 않습니다. 자산을 추가·수정·삭제하면 예측의 전제가 바뀌므로 `invalidateDividendForecast()`로 캐시를 버리고 화면에 떠 있는 곳은 즉시 다시 불러옵니다(이전 값을 지우지 않고 갱신해 깜빡임 없음). 목 서버(티커당 300ms 지연·11개 티커) 측정 결과 **3,674ms → 669ms**, 출력은 완전히 동일했고 화면 이동 4회당 요청은 4건 → **1건**으로 줄었습니다.
- **주식 집중 분석**: 보유 자산 중 **주식만** 모아 분석하는 전용 메뉴 (소유자 필터·통화 토글·**같은 종목 합산** 토글 연동 — 포트폴리오와 같은 preference를 공유해 같은 티커 포지션을 하나로 합쳐 분석하며, 합산 종목은 상세에서 수정 대신 '합산' 배지로 표시) — ① **섹터 구성 도넛**(기술·금융·헬스케어·소비재·통신 등, 하이브리드 분류: 자산 폼에서 수동 지정하거나 티커로 Yahoo `quoteSummary`에서 자동 조회 → `stock-sector.ts` 정규화, 저장은 마이그레이션 불필요한 `sector?` 필드. 필요 시 **세부 섹터**를 자유 입력해 `기술 (반도체)`·`기술 (AI SW)`처럼 배지·상세에 표시할 수 있으며 — 마이그레이션 불필요한 `subSector?` 필드. **섹터 도넛은 세부 섹터가 입력된 경우 이를 별도 슬라이스로 나눠 비중을 계산**해 특정 섹터가 압도적으로 클 때 더 잘게 구분할 수 있고, 색은 기본 섹터 색의 명암 변주로 같은 계열끼리 묶어 보여줍니다), ② **지역(국내/해외)·통화(₩/$) 노출** 막대, ③ **집중도 지표**(Top 5 비중·실질 종목 수·HHI 기반 분산 점수, 편중 경고), ④ **손익 기여**(수익/손실 기여 1위), ⑤ **종목별 표**(섹터 배지·비중·평가금액·손익·기여도, 행 클릭 시 상세, 헤더 클릭 정렬 — 섹터 열은 **세부 섹터까지 포함한 텍스트 기준**으로 정렬해 같은 섹터·세부 섹터끼리 묶입니다), ⑥ **밸류에이션 비교 표**(PER·PBR·시가총액·배당수익률·52주 위치 — Yahoo `quoteSummary`에서 티커 배치 조회, `/api/stocks/valuations` + `useStockValuations` 훅, 1시간 캐시, 조회 실패 시 안전 폴백), ⑦ **리스크 분석**(최근 1년 일간 시세 기반 — 종목별 **연율화 변동성·1년 수익률·최대 낙폭·52주 위치** 표 + **일간 수익률 상관관계 히트맵**(공통일 정렬, 빨강=동조·파랑=역행). `/api/stocks/history` + `useStockHistory` 훅, 계산은 `stock-risk-service.ts`, 6시간 캐시). 밸류에이션·리스크 표도 헤더를 클릭해 각 지표(PER·배당수익률·변동성·수익률·52주 위치 등) 기준으로 정렬할 수 있습니다(값이 없는 행은 항상 뒤로). 종목별 표는 다른 리스트처럼 기본 Top 5(비중 순) + **더보기**로 접히고, 행을 클릭하면 ⑧ **개별 종목 드릴다운 모달**이 열립니다 — 보유 손익(수량·평단가·현재가·원금), **가격 차트(1개월/3개월/1년 토글)**, 52주 위치·변동성·1년 수익률·최대 낙폭, PER·PBR·시총·배당수익률 지표, 수정 바로가기. 계산은 `stock-analysis-service.ts`로 분리하고, 스켈레톤·빈 상태·i18n을 갖춥니다.
- **금액 숨기기(프라이버시 모드)**: 사이드바 하단의 눈 아이콘을 누르면 모든 화면의 금액이 `•••••`로 가려집니다 — 총자산·손익·테이블·도넛·차트 축·툴팁까지 전부 포함하되, 수익률(%)·비중·차트 곡선 모양은 그대로 남겨 화면 공유 시에도 흐름은 파악할 수 있습니다. 선택값은 `localStorage`에 저장되어 새로고침 후에도 유지됩니다 (자산 입력 폼의 금액은 가리지 않음).
- **스켈레톤 로딩**: 데이터를 불러오는 동안 각 페이지(대시보드/포트폴리오/분석/배당)는 실제 레이아웃과 같은 형태의 스켈레톤(은은한 pulse)을 표시합니다 — 제목은 즉시 렌더되고 카드·차트·테이블 자리는 회색 블록으로 채워, 콘텐츠가 로드될 때 레이아웃이 튀지 않고(no layout shift) 체감 로딩이 빨라집니다. 배당 예측·다가오는 배당 카드는 외부 조회 중 별도 스켈레톤을 보여줍니다.
- **빈 상태 & 온보딩**: 자산이 하나도 없는 첫 실행 시 대시보드는 ₩0 카드 대신 **환영 화면**(NEXUS 소개 + 기능 안내 카드 + "첫 자산 추가하기" CTA)을 보여줍니다. 포트폴리오·분석·배당도 비어 있을 때 안내 문구와 바로가기 버튼이 있는 빈 상태 카드를 표시하며, "자산 자체가 없음"과 "소유자 필터 결과가 없음"을 구분해 각각 다른 메시지(예: "OO 명의의 자산이 없어요 — 필터를 바꾸거나 자산을 추가해 보세요")를 안내합니다.
- **마이크로 인터랙션**: 주요 금액(총자산·투자원금·평가손익·분석 스탯 타일·배당 스탯/예상 배당)은 로드·값 변경 시 0(또는 이전 값)에서 목표값까지 **부드럽게 카운트업**(ease-out)합니다. 프라이버시 마스킹 중이거나 `prefers-reduced-motion`이 켜진 경우, 값이 그대로일 때는 애니메이션을 건너뜁니다. 페이지 이동 시에는 콘텐츠가 살짝 **페이드인**되어 전환이 매끄럽습니다.
- **대시보드**: 총 자산 요약 카드 + 자산 구성 도넛 차트(2:1 그리드, 세그먼트/범례 호버 시 중앙에 카테고리별 금액·비중 표시), **"오늘 챙길 것" 액션 센터**, **목표 배분·리밸런싱 카드**, 총 자산 추이 차트, 보유 자산 미리보기
- **"오늘 챙길 것" 액션 센터**: 인사이트가 7개 메뉴에 흩어져 있어 매번 돌아다녀야 알 수 있던 문제를 해결하는 대시보드 카드입니다 — 리밸런싱 이탈, 금융소득종합과세 기준 임박/초과, 배당 입금 예정(D-14 이내), 그리고 11~12월에만 등장하는 **연말 절세 체크리스트**(해외주식 양도세 기본공제 미사용분, 손실 확정 후보 종목 수, 연금 세액공제 잔여 한도)를 한 곳에 모아 **심각도 순으로 정렬**해 보여주고, 각 항목을 누르면 해당 메뉴로 이동합니다(리밸런싱 카드는 대시보드 자기 자신에 있어 `/`가 아니라 `/#rebalance` 해시로 카드 위치까지 스크롤). 정렬은 심각도(긴급/주의/정보) → 카테고리 순인데, 세금·연금처럼 **연말이 지나면 기회가 사라지는 항목이 리밸런싱보다 앞**에 오도록 했고, 리밸런싱은 6개 자산군에서 동시에 뜰 수 있어 **이탈 큰 3개만** 노출해 마감 있는 항목을 가리지 않게 했습니다. 조치할 일이 없으면 초록 체크와 함께 빈 상태를 보여줍니다. 새로 계산하는 값은 없고 기존 `rebalance-service`·`tax-service`·배당 예측 결과를 조합하기만 하며, 로직은 `action-center-service.ts`에 분리했습니다.
- **목표 자산배분 & 리밸런싱**: "지금 이렇게 되어 있다"에서 한 걸음 더 나아가 **"그래서 뭘 해야 하나"**에 답하는 기능입니다. **설정 → 목표 자산배분**에서 목표 비중을 정하면(안정형/중립형/성장형/공격형 프리셋 제공 — 고전적 60/40 균형 포트폴리오와 국내:해외 주식 약 1:2 분산을 참고한 출발점이며, 슬라이더·숫자 입력으로 **카테고리별로 직접 조정** 가능. 합계가 100%가 아니면 저장이 막히고 부족/초과분을 안내), 대시보드 카드가 카테고리별 **현재 비중 막대 + 목표 지점 마커 + 이탈(%p)**을 보여줍니다. 이탈 판정은 **5/25 규칙**을 따릅니다 — 절대 허용 범위(기본 ±5%p, 설정에서 조정)와 **목표 비중의 25%** 중 더 엄격한 쪽을 적용하므로, 비중이 작은 자산일수록 더 민감하게 잡힙니다(예: 목표 10%면 ±2.5%p). 범위를 벗어나면 **"목표를 맞추려면 한국주식 ₩9,338,481 매수 · 현금 ₩24,786,880 매도"**처럼 금액까지 계산해 제시하고, **"새 자금으로만 맞추기"**에 추가 투자금을 입력하면 **매도 없이 부족한 자산군에만 비례 배분**하는 안(세금·거래비용 절약)을 함께 보여줍니다. 목표는 `settings`에 저장되어 백업에 포함되고, 토글로 기능 전체를 끌 수 있습니다. **기타(CUSTOM) 자산 추가/수정 폼에서 "실거주 주택(집)이에요" 체크박스**로 특정 자산을 집으로 표시할 수 있고, 설정의 **"실거주 주택(집) 제외"** 토글을 켜면 그 자산을 목표 배분 계산에서 완전히 빼고 나머지 비중을 다시 계산합니다 — 유동성 없는 실거주 주택이 자산군 비중을 왜곡하는 문제를 막기 위함이며, 대시보드/포트폴리오 등 다른 화면의 총자산·비중에는 영향을 주지 않고 리밸런싱 계산에만 적용됩니다. 계산은 `rebalance-service.ts`, 프리셋·검증은 `target-allocation.ts`에 분리했습니다.
- **포트폴리오**: **한국주식 / 해외주식 / 채권 / 현금 / 연금 / 기타** 카테고리별 섹션. 주식의 국내/해외 구분은 시장(KOSPI·KOSDAQ·KRX) → 티커 형식(6자리 숫자) → 통화 순으로 자동 판별하며 저장 스키마 변경 없이 파생됩니다. 각 섹션에 카테고리 합계, **비중 Top 5 가로 바 차트**, 그리고 카테고리 내 비중(%) 컬럼이 추가된 테이블이 표시됩니다 (행 클릭/연필로 수정, 휴지통으로 삭제, USD 자산 배지). 테이블 헤더(자산·투자 원금·평가 금액·비중·평가 손익)를 클릭하면 해당 컬럼 기준으로 내림/오름차순 정렬됩니다. 이 헤더 클릭 정렬은 **주식·배당 메뉴의 표에도 공통 적용**되며(공용 `SortableHeader`), 숫자뿐 아니라 종목명·섹터 같은 **텍스트 열도 정렬**해 같은 텍스트끼리 묶입니다(값 없는 행은 뒤로)
- **분석**: 일별 스냅샷을 활용한 통계 페이지 — 전체/1개월/3개월 수익률과 최대 낙폭(MDD) 스탯 타일, 월별 수익률 막대 차트(월말 평가액의 전월 대비 변화), 카테고리별 평가 손익 다이버징 바, 전체 기간의 평가 금액 vs 투자 원금 라인 차트(크로스헤어 툴팁)
- **배당**: 배당·이자 수령 기록 관리 — "배당 추가" 다이얼로그에서 **종목별 입력**(종목명 자동완성, ₩/$ 금액, **소유자**, 지급일, 메모)과 **전체 한번에**(일괄 입력) 두 가지 모드를 고를 수 있습니다. 보유 종목이 많으면 받은 배당을 종목마다 찾아 넣기가 번거로우므로, 일괄 모드에서는 종목명 대신 기간 표시(예: `2026`)만 붙여 **합계 금액을 한 건으로 기록**합니다 — 통계·차트·세금 계산에는 종목별 입력과 똑같이 반영되고 종목별 상세만 남지 않습니다. 이렇게 입력하고, 올해 누적·최근 12개월·월 평균·이번 달 스탯 타일(각각 컬러 아이콘 배지로 구분: 블루·그린·앰버·퍼플)과 최근 12개월 월별 배당 막대 차트, 월별 그룹 목록(소유자 배지·삭제 가능)으로 확인합니다. 배당에도 소유자 태그가 있어 상단의 **전체/본인/배우자/자녀/공동 필터**로 통계·차트·목록·예측이 모두 해당 소유자 기준으로 재계산됩니다. 자산과 별개의 이벤트 기록이라 매도한 종목의 과거 배당도 그대로 남습니다
- **배당 자동 분석 (입력 불필요)**: 보유 주식의 티커로 외부 배당 이력(`/api/dividends/forecast` → 야후 차트 API `events=div`, 1시간 캐시)을 조회해 자동 표시합니다 — ① **예상 연간 배당**: 최근 12개월 주당 배당 × 현재 보유 수량(세전 추정, 원화 환산), **세후 추정(원천징수 15.4% 가정)**, 포트폴리오 배당수익률(그린 색 배지), 종목별 주당 배당·예상 수령액·시가 배당률 테이블(예상 수령액 큰 순 Top 5 기본 표시, 6개 이상이면 **더보기**로 펼침). ② **다가오는 배당**: 과거 지급 주기를 추정해 다음 배당락 예상 시기(D-day)와 금액(앰버 색 캘린더 아이콘, 가까운 순 Top 5 기본 표시, **더보기**로 나머지 펼침). ③ **받은 배당 기록 제안**: 외부 이력의 최근 지급 건 중 아직 기록하지 않은 것을 찾아 **원클릭으로 기록에 추가**(이미 기록된 건은 종목명+월 기준 자동 제외). 모두 추정임을 명시하고 조회 실패 종목은 표기됩니다
- **세금**: 보유 현황·배당 기록만으로 계산하는 세금 시뮬레이터(모두 클라이언트에서 계산, 외부 API 불필요) — 실제 세액이 아닌 추정치임을 페이지 전체에 명시합니다. **소유자(본인/배우자/자녀/공동)별로 완전히 분리해서 계산**합니다 — 해외주식 양도세 연 250만원 공제·금융소득종합과세 2천만원 기준·연금 세액공제 한도가 모두 개인 단위로 적용되는 항목이라, 가구 전체를 합산해 계산하면 실제와 다른(대개 더 위험해 보이는) 결과가 나오기 때문입니다. 자산·배당이 없는 소유자는 섹션 자체가 표시되지 않습니다. **공동(JOINT) 태그는 세금 화면에서 아예 제외**했습니다 — 세법상 '공동' 귀속은 존재하지 않고 실제로는 어느 한 사람 명의의 계좌이므로, 공동으로 태그된 항목이 있으면 상단에 "실제 명의자로 소유자를 바꾸면 그 사람 계산에 반영된다"는 안내만 보여줍니다. ① **해외주식 양도소득세 시뮬레이터**: 250만원 기본공제는 **올해 실현손익 전체 합계**에 적용되므로, 보유 종목의 평가손익만으로는 정확한 세액을 알 수 없습니다 — 그래서 **"올해 실현손익(이미 매도해서 확정된 금액)"을 이익/손실 부호와 함께 직접 입력**받고, 여기에 보유한 **해외 상장 주식만**(국내 상장 주식은 소액주주 양도차익이 원칙적으로 비과세라 대상에서 제외)을 체크박스로 선택/해제해 "추가로 더 판다면"을 얹어 계산합니다. 실현손익(입력)·추가 매도 손익(선택)·합계 손익·남은 기본공제·과세 대상 차익·예상 세액(22%, 국세+지방세)을 6개 타일로 보여주고, 이미 실현손익만으로 세금이 발생하는 경우 "추가로 팔지 않아도 이미 얼마, 추가로 팔면 얼마가 더 늘어난다"를 문구로 안내합니다. 이미 실현한 손실이 있으면 신규 평가이익을 상쇄해 공제 한도를 아끼는 효과가, 반대로 이미 실현한 이익이 있으면 공제 한도가 먼저 소진되는 효과가 정확히 반영됩니다. 평가손실 종목은 **손실 확정 후보** 배지로 표시해 공제 한도를 채우는 손실 매도(tax-loss harvesting) 판단을 돕습니다. ② **금융소득종합과세 트래커**: 배당 메뉴에 기록된 올해 배당·이자 합계(이표 이자도 배당 기록으로 함께 관리되므로 자동 포함)에 **앱에 기록하지 않은 배당·이자를 보정하는 수동 입력**을 더해 "지금까지"를 계산하고, 여기에 **연말까지 예상 배당**을 얹은 "연말 예상"까지 두 게이지로 함께 보여줍니다. 연말 예상은 배당 메뉴의 배당 예측 서비스(`dividend-forecast-service.ts`, 종목별 다음 지급 예정일·지급 주기·최근 주당 배당액)를 재사용해, 오늘부터 12월 31일까지 같은 주기로 몇 번 더 지급될지 계산하고 매번 최근 지급액이 반복된다고 가정합니다(배당률 변동이나 신규 종목 취득은 반영하지 않음 — 명시적으로 밝힌 단순화 가정). 두 게이지 모두 2천만원 기준금액과 비교해 초과 시(또는 이대로면 연말에 초과할 것으로 예상될 시) 각각 안내 문구를 보여줍니다. ③ **연금 세액공제 계산기**: 연금저축·IRP 올해 납입(예정)액을 입력하면 각각의 인정 한도(연금저축 600만원/합산 900만원)와 소득 구간별 공제율(16.5%/13.2%)로 예상 세액공제액과 남은 한도를 계산합니다(현재 데이터 모델은 연금 누적 납입원금만 저장해 올해분을 자동 도출할 수 없어 직접 입력받는 순수 계산기이며, 보유 연금 계좌는 참고용으로 하단에 나열). ④ **연금 수령 시뮬레이터**: ③(세액공제, 넣을 때)의 반대편(받을 때) — 예전에는 이 절반이 비어 있었습니다. 보유한 사적연금(연금저축·IRP) 잔액을 **수령 기간에 걸쳐 나눠 받는 연금(annuity)**으로 환산해 월 수령액·월 실수령액·연금소득세를 추정합니다(잔액은 자산에서 자동 집계, 수령 시작 나이·기간·수령 중 수익률만 입력). 핵심은 **연 1,500만원 저율 분리과세 한도**입니다 — 연 수령액이 이 아래면 연령별 저율(55~69세 5.5%·70대 4.4%·80세~ 3.3%, 수령 기간에 걸쳐 나이대별로 가중평균)로 종결되고, 넘으면 종합과세 또는 16.5% 분리과세 대상이 됩니다. 그래서 **한도를 넘는 경우 "수령 기간을 N년 이상으로 늘리면 한도 아래로 유지된다"는 절세 가이드**(그 N을 역산해서)를 함께 보여줍니다 — 은퇴 진단의 세 레버와 같은 실행 가능한 처방입니다. 과세 대상은 잔액 전체로 단순 가정(세액공제 안 받은 원금이 있으면 실제는 더 적음)임을 명시하고, 국민연금은 데이터가 없어 제외합니다. 보유 연금 자산이 있는 소유자에게만 표시됩니다. 계산은 `tax-service.ts`, 세율·한도 상수는 `tax.ts`에 분리했습니다. **직접 입력한 값(실현손익·부호, 미기록 배당, 연금저축·IRP 납입액, 소득 구간, 양도세 시뮬레이션 체크 해제 종목)은 설정에 저장**됩니다 — 예전에는 컴포넌트 `useState`에만 있어서 새로고침이나 화면 이동만으로 사라졌고, 빈 값은 "아직 입력 안 함"이 아니라 **"세금 0원"처럼 보이는** 게 문제였습니다. 저장 키는 **연도 + 소유자**인데 둘 다 의미가 있습니다: 모든 금액이 그 해의 값이라 해가 바뀌면 작년 숫자가 딸려오면 안 되고(새 연도는 기본값에서 시작, 과거 5년치만 보관), 공제·기준금액은 개인 단위라 소유자별로 갈라야 합니다. 입력은 화면에 즉시 반영되고 저장은 0.7초 디바운스로 묶어 타이핑마다 PUT을 날리지 않으며, 페이지를 떠날 때(`pagehide`) 남은 변경을 마저 보냅니다. 설정은 JSON 블롭으로 저장되고 읽을 때 정규화되므로 **마이그레이션이 필요 없고 백업/복원에도 자동 포함**됩니다. 저장된 '미기록 배당'은 대시보드 **"오늘 챙길 것"의 금융소득 경고에도 반영**돼, 세금 화면과 대시보드가 서로 다른 금액을 말하지 않습니다.
- **은퇴 준비**: "얼마 있나"를 넘어 **"충분한가"**에 답하는 목표 기반 진단 메뉴 — 대부분의 재료(보유 자산, 최근 저축 페이스, 자산 추이)는 앱이 이미 갖고 있으므로, 사용자에게는 **출생연도·은퇴 목표 나이·은퇴 후 월 생활비·기타 월 수입(국민연금 등)·기대 수익률·물가상승률·기대 수명** 정도만 받습니다(모두 **오늘 물가 기준**으로 입력 — 미래 금액을 상상할 필요가 없습니다). 상단의 **전체/본인/배우자/자녀** 필터로 범위를 고르며(은퇴는 개인 단위라 세금 메뉴와 같은 사고방식), 입력값은 필터별로 설정에 자동 저장(0.7초 디바운스 + `pagehide` 플러시, JSON 블롭이라 마이그레이션 불필요·백업 자동 포함)됩니다. ① **은퇴 진단**: 헤드라인은 잔액이 아니라 **"돈이 몇 살까지 버티는가"**입니다("약 88세까지 버팁니다" / "약 78세에 바닥납니다") — 사람이 곧바로 행동으로 옮길 수 있는 지표라서입니다. 함께 **목표 달성률 게이지**, 은퇴 시점 예상 자산과 필요 자산을 보여주는데, **모든 계산은 실질(오늘 물가 기준)**로 처리합니다 — 25년 뒤 명목 20억을 "충분하다"고 착각하게 만들지 않기 위해서이고, 명목값은 보조로만 병기합니다. 부족하면 **세 개의 레버**(월 저축을 얼마 더 / 은퇴를 몇 년 늦추면 / 생활비를 얼마 줄이면 목표에 닿는지)를 각각 계산해 제시합니다. ② **저축 페이스 자동 감지**: "월 얼마 저축하세요?"라고 물으면 대부분 부정확하게 답하지만, 앱은 이미 답을 압니다 — 스냅샷의 **원금 순증**이 곧 순납입액입니다. 최근 월별 원금 증가의 **중앙값**(평균이 아님 — 자산 뒤늦은 등록·평단가 수정 같은 일회성 급등에 흔들리지 않도록)을 기본 저축액으로 제시하고, 이력이 들쭉날쭉하면 그 사실을 표시하며 언제든 수동 입력으로 덮어쓸 수 있습니다. ③ **시나리오 밴드 차트**: 단일 숫자의 거짓 정밀도 대신 **보수/기본/낙관 세 수익률 가정**을 밴드로 그리고, **과거 실제 자산(스냅샷)에서 은퇴 시점까지 이어 붙여**(경계에 '지금' 점선) 필요 자산(명목) 목표선과 함께 보여줍니다 — 몬테카를로("성공 확률 87%")는 가정 대비 과한 정밀도라 일부러 배제한 결정론적 3밴드입니다. 계산은 전부 순수 함수(`retirement-service.ts`)로 분리해 손으로 검증한 값(실질 수익률 0% 케이스 등)으로 테스트했고, 세율·한도 없이 오늘 자산·저축·물가만으로 계산하므로 외부 API가 필요 없습니다.
- **뉴스**: 국내 주요 경제·투자 뉴스를 **최근 24시간** 기준으로 모아 보여주는 메뉴 — 연합뉴스·한국경제·매일경제·이데일리·전자신문 등 시황(증권)·IT/반도체 위주 출처의 공개 RSS를 서버에서 병렬 수집해(`news-service.ts` → `/api/news`, 15분 캐시) 24시간 이내로 필터링하고 제목 기준 중복 제거 후 최신순 정렬합니다. **일반 경제 개괄 뉴스는 제외**하고, **보유 포트폴리오 기준 관련도 필터**를 추가로 적용합니다 — 항상 관심 대상인 반도체·AI 앵커 키워드에 더해, 보유 주식 중 **비중이 큰 섹터**(세부 섹터 포함, ≥8%)와 **비중이 큰 개별 종목**(이름·티커·주요 해외 티커의 한글 표기)을 키워드로 추출해 기사 제목·요약에 매칭되는 것만 남깁니다(포트폴리오가 비어 있으면 반도체·AI 앵커만 적용). 이 관련도 필터는 포트폴리오가 바뀌면 피드 캐시와 무관하게 즉시 반영됩니다. **기본 10개**를 보여주고 **더보기**를 누르면 10개씩(→20개…) 펼쳐지며 접을 수 있습니다(다른 리스트와 동일한 기조). 각 항목은 **출처별 고유 색 배지**(왼쪽 색 accent 포함)·상대 시각(`5분 전`/`2시간 전`)·제목(새 탭 링크)·요약으로 구성되고, 스켈레톤·빈 상태·i18n을 갖추며 일부 출처 조회 실패 시 하단에 안내합니다. 외부 데이터는 읽기 전용이라 저장하지 않으며, RSS 호스트로의 아웃바운드 접근이 막힌 환경에서는 빈 상태로 안전하게 폴백합니다(테스트는 `NEWS_API_BASE`로 목 서버 지정).
- **소유자 구분 (가족 공용 모드)**: 로그인 없이 자산마다 소유자 태그(**본인/배우자/자녀/공동**)를 붙여 분리 관리합니다. 자산 추가/수정 폼에서 소유자를 선택하고(기존 데이터는 미지정 시 공동 처리 — 마이그레이션 불필요), 대시보드/포트폴리오/분석 우측 상단의 **전체/본인/배우자/자녀/공동 필터**로 모든 화면(총자산·도넛·테이블·카테고리 손익·**총 자산 추이 및 분석 전체**)이 해당 소유자 기준으로 재계산됩니다. "전체"가 가족 합산 뷰이고, 포트폴리오 각 자산에는 소유자 배지가 표시됩니다. 필터 선택은 localStorage에 유지됩니다.
- **소유자 이름·색상 커스터마이즈 (설정)**: **설정 → 소유자 태그**에서 각 소유자의 표시 이름(예: 본인→"성민", 자녀→"하윤")과 **색상**을 바꿀 수 있습니다. 색은 컬러 피커로 자유롭게 고르며(행마다 실시간 미리보기 배지 + 기본색 되돌리기 버튼), **앱 전체**에 반영됩니다 — 자산·배당 배지, 소유자 도넛/범례, 성과 카드 점, **소유자 필터 칩의 색 점**, **자산·배당 추가 폼의 소유자 선택 점**, 합산 뷰의 소유자 이름, 그리고 특정 소유자로 필터링했을 때 **대시보드 총자산 추이선·분석 성장 그래프의 평가선 색**까지 모두 해당 소유자 색을 따릅니다(임의 색도 인라인 스타일로 동작). 기본색은 라이트·다크 모드 모두에서 잘 보이도록 검증된 4색 팔레트이고, 이름·색 모두 `settings` 테이블에 저장되어 백업에도 포함됩니다. 자녀 항목을 쓰지 않으면 이름은 비워두면 되고(기본 이름으로 표시), 자녀 태그로 자녀 명의 자산·배당도 별도 관리할 수 있습니다.
- **소유자별 히스토리 & 분석**: 일별 스냅샷에 소유자별 원금·평가금액(`byOwner`)을 함께 기록하므로, 소유자 필터를 켜면 총 자산 추이·기간 수익률·최대 낙폭·성장 차트가 그 사람 기준으로 표시됩니다(소유자별 기록이 없는 과거 구간은 현재 비중으로 추정하며 안내). 분석 페이지 "전체" 뷰에는 소유자별 평가금액·손익을 나란히 비교하는 **소유자별 성과** 카드가 추가됩니다.
- **소유자별 구성 도넛**: 대시보드에 소유자별 총자산 비중 도넛이 추가됩니다 — 소유자 필터와 무관하게 항상 전체(합산) 기준으로 "우리 집 자산에서 각자의 몫"을 보여줍니다.
- **같은 종목 합산 뷰**: 포트폴리오의 "같은 종목 합산" 토글을 켜면 같은 티커(+통화)의 주식이 한 줄로 병합됩니다 — 수량 합산, 평단가는 보유량 가중평균, 소유자 배지 복수 표시, 서브라인에 "N건 합산: 본인 30주 · 배우자 10주" 내역 표기. 합산 행은 읽기 전용이며(수정/삭제는 합산 해제 후), Top 비중 차트도 합산 기준으로 갱신됩니다. 토글 상태는 localStorage에 유지됩니다.
- **표시 통화 전환**: 대시보드/포트폴리오/분석 우측 상단의 ₩/$ 토글로 모든 금액(요약·도넛·추이 차트·테이블)을 원화 또는 달러로 표시. 달러 모드에서는 적용 환율($1 = ₩N)을 함께 표기하며, 선택은 localStorage에 저장됩니다. 환율 조회(`/api/fx`)에 실패하면 기본값(₩1,400)으로 폴백하고 그 사실을 표시합니다.

## 자산 추가 폼 — 필수/선택 항목과 현재가 자동 조회

자산 추가 시 필수 입력은 최소한으로 유지됩니다:

| 종류 | 필수 | 선택 |
|---|---|---|
| 주식 | 종목명, 보유 수량 | 티커, 시장, 평단가 |
| 채권 | 자산명, 매입 금액 | 현재 평가 금액(미입력 시 매입 금액), 표면금리, 만기일 |
| 현금 | 자산명, 금액 | - |
| 연금 | 자산명, 납입 원금 | 계좌 유형(DC·IRP·연금저축 등 자유 입력), 현재 평가 금액 |
| 기타 | 자산명, 매입 금액 | 카테고리(부동산·금·암호화폐 등 자유 입력), 현재 평가 금액 |

모든 자산은 **원화(₩)/달러($) 중 입력 통화를 선택**할 수 있습니다. 금액은 입력한 통화 그대로 저장되고, 평가/합산 시점에 실시간 USDKRW 환율(야후 파이낸스)로 원화 기준 환산됩니다 — 환율이 움직이면 달러 자산의 원화 평가액도 함께 움직입니다.

**주식 시세는 저장 시점 외에도 언제든 일괄 갱신할 수 있습니다**: 대시보드/포트폴리오 헤더의 "시세" 버튼을 누르면 보유 종목 전체의 현재가를 다시 조회해 반영하고(`POST /api/assets/refresh-prices`, 종목 통화 기준으로 저장), 갱신되면 오늘 스냅샷도 함께 기록됩니다. 앱에 처음 접속할 때 세션당 1회 자동으로도 갱신되며, 마지막 갱신 시각이 버튼 옆에 표시됩니다. 조회 실패 종목이 있어도 나머지는 정상 갱신됩니다.

**주식의 현재가는 입력받지 않고 저장 시 자동 조회합니다** (`/api/quote` → 야후 파이낸스 차트 API, API 키 불필요):

- 국내 6자리 코드는 KOSPI(`.KS`)/KOSDAQ(`.KQ`)으로, 알파벳 티커는 미국 시장으로 매핑됩니다.
- 미국 주식은 USDKRW 환율로 원화 환산해 저장합니다 (앱 전체가 KRW 기준).
- 조회 우선순위: 티커 조회 성공값 → 평단가 → (수정 시) 기존 저장값. 전부 없으면 안내 문구와 함께 저장이 중단됩니다. 평단가를 비우면 조회된 현재가로 채워집니다(손익 0에서 시작).
- 시세는 서버에서 5분간 캐시되며, 테스트/차단 환경에서는 `QUOTE_API_BASE` 환경변수로 엔드포인트를 교체할 수 있습니다.

## 자산 히스토리 & 추이 차트

일별 스냅샷(`snapshots` 테이블, 날짜당 1행)으로 총 자산 추이를 기록합니다:

- **기록 시점**: 자산을 추가/수정/삭제할 때마다, 그리고 대시보드 접속 시(하루 1회 갱신) 오늘 날짜의 스냅샷이 현재 자산 상태로 덮어써집니다.
- **데모 히스토리**: DB가 비어 있으면 최초 접속 시 현재 자산 가치를 기준으로 90일치 히스토리를 생성해 시딩합니다(고정 시드 난수 워크 — 데모용). 이후 실제 기록이 쌓이면서 대체됩니다.
- **차트**: 대시보드의 "총 자산 추이" 카드. 1개월/3개월/전체 기간 탭, 구간 손익 요약, 호버 시 크로스헤어 + 날짜별 평가금액/전일 대비 툴팁, "표로 보기"로 원본 수치 테이블 확인 가능. 외부 차트 라이브러리 없이 SVG로 구현되어 있습니다.

## 데이터 백업 (설정 페이지)

데이터는 로컬 `data/nexus.db`에만 저장되므로, **설정** 페이지에서 JSON 백업을 관리할 수 있습니다:

- **백업 내보내기**: 모든 자산·배당·자산 추이(스냅샷)와 설정(소유자 이름)을 `nexus-backup-YYYY-MM-DD.json` 파일 하나로 다운로드합니다. 클라우드 드라이브 등 안전한 곳에 보관하면 데이터가 초기화돼도 복원할 수 있습니다.
- **백업 가져오기**: 내보낸 JSON을 업로드해 복원합니다. 서버에서 형식·무결성을 검증(`app`/`version`/각 레코드)한 뒤, id·생성일·소유자 등 원본 필드를 **그대로 보존**해 되살리며, 백업에 포함된 소유자 이름 설정도 함께 복원합니다(설정이 없는 구버전 백업은 기본 이름 유지).
- 가져오기는 **현재 데이터를 전부 대체**(병합 아님)하므로 확인 창을 거치며, 복원 후 화면이 자동 새로고침됩니다.

> 이 방식은 파일 기반 수동 백업입니다. 컨테이너 재시작 등으로 데이터가 초기화될 수 있는 환경에서는 정기적으로 내보내 두거나, 근본적으로는 외부 영속 DB(Turso 등)로 전환하는 것을 권장합니다.

### 반복 테스트를 위한 초기화

자산 데이터는 SQLite 파일(`data/nexus.db`)에 저장되어 서버를 재시작해도 유지됩니다. 실제 자산 데이터가 쌓인 뒤 실수로 초기화되는 사고를 막기 위해 사이드바의 초기화 버튼은 제거했습니다.

시나리오를 처음부터 다시 테스트하고 싶다면 **실제 DB를 건드리지 말고 별도 경로에 띄우는 것을 권장**합니다:

```bash
NEXUS_DB_PATH=/tmp/nexus-test.db NEXUS_SEED=1 PORT=4000 npm run dev
```

실제 DB 자체를 데모 상태로 되돌리려면 `data/nexus.db`를 삭제한 뒤 `NEXUS_SEED=1`로 실행하면 됩니다(삭제 전 **설정 → 백업 내보내기**를 먼저 하세요).

### 확인해볼 시나리오 예시

1. 사이드바 "자산 추가" → 자산 종류(주식/현금) 선택 후 신규 자산 등록 → 대시보드/포트폴리오에 반영되는지 확인
2. 포트폴리오 테이블에서 자산 행 클릭(또는 행 우측 연필 버튼) → 수정 다이얼로그에서 보유 수량 등 값을 바꿔 저장 → 원금/평가금액/손익이 재계산되어 반영되는지 확인 (예: 30주 → 5주 감소, 5주 → 20주 증가)
3. 포트폴리오 테이블 각 행 우측의 휴지통 버튼(또는 수정 다이얼로그 하단의 삭제 버튼)으로 자산 삭제 → 확인 창 후 목록과 대시보드 총 자산에서 제거되는지 확인
4. 대시보드의 "자산 구성" 비중 그래프가 종류별로 갱신되는지 확인
5. 포트폴리오 페이지에서 주식/현금이 섹션별로 그룹핑되어 보이는지 확인
6. 새로고침(F5) 또는 서버 재시작 후에도 위 변경 사항이 유지되는지 확인
7. 사이드바 하단 아이콘으로 다크/라이트 모드 전환
